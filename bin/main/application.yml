server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sys?autoReconnect=true&useSSL=false
    username: root
    password: abcd@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

    
mybatis:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.example.play.domain
